import { takeEvery } from 'redux-saga/effects';
import * as types from '../constants/ActionTypes.js'

export const sendBid = function* sendBid(params) {
	console.log('ADD ME in src/sagas/index.js',action);
}
export const sendAccept = function* sendAccept(params) {
	console.log('ADD ME in src/sagas/index.js',action);
}
